
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <a class="navbar-brand" href="/">HobbyBuddies</a>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <% if user_signed_in? %>
                        <%= link_to 'Login', new_user_registration_path, class: "nav-link" %> 
                    <% else %>
                        <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %> 
                    <% end %>
                </li>
                <li class="nav-item">
                    <% if user_signed_in? and current_user.profile.blank? %>
                        <%= link_to 'Profile', new_profile_path, class: "nav-link" %>
                    <% elsif user_signed_in? %>
                        <%= link_to 'Profile', profile_path(current_user.profile), class: "nav-link" %>
                    <% else %>
                        <%= link_to 'Profile', new_user_session_path, class: "nav-link" %>
                    <% end %> 
                </li>
                <li class="nav-item">
                    <%= link_to 'My Listings', my_listings_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Search', root_path, class: "nav-link" %>
                </li>
                </ul>
                <span class="btn btn-bd-download d-none d-lg-inline-block mb-3 mb-md-0 ml-md-3">
                <%= link_to "Logout", destroy_user_session_path, method: "DELETE" if user_signed_in? %>
                </span>
            
    </nav>
</div>
